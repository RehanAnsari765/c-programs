#include <stdio.h>

void main () {

    int wallet = 500;

    if(wallet >= 500 && wallet <= 1000) {
        printf("Money is enough");
    }else {
        printf("Money is not enough");
    }

}
